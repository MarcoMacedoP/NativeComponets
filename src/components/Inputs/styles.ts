import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginVertical: 4,
    borderWidth: 2,
    borderStyle: 'solid',
    paddingVertical: 6,
    paddingHorizontal: 6,
    borderRadius: 4,
  },
});
